
export type Url = String;

export type Disk = {
    id: Number,
    name: String,
    description: String,
    janre: String,
    author: String,
    picture: Url,
    price: Number,
    actionPrice: Number,
    currency: String,
    popularityInd: number,
}
